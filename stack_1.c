#include "monty.h"

/**
 * f_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
 */
void f_stack(stack_t **head, unsigned int counter)
{
    (void)head;
    (void)counter;
    /* Assuming bus and lifi are global variables or structs defined elsewhere */
    bus.lifi = 0;
}

